# ============================================================================
# AI Audit Assistant — runtime dependencies
# Installed incrementally per build phase. Phase tag in the comment.
# ============================================================================

# --- Phase 0: core / config / UI shell -------------------------------------
streamlit==1.41.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Phase 1: document parsing ---------------------------------------------
pymupdf==1.25.1            # PDF text + structure (fitz)
pdfplumber==0.11.5         # PDF table extraction
python-docx==1.1.2         # Word
openpyxl==3.1.5            # Excel .xlsx
xlrd==2.0.1               # Excel legacy .xls
pandas==2.2.3             # tabular handling
Pillow==10.4.0           # images (10.3<=x<11 required by fastembed)
python-magic-bin==0.4.14 ; sys_platform == "win32"   # MIME sniffing on Windows

# --- Phase 2: RAG core ------------------------------------------------------
# Qdrant local mode = pure-python, no C++ compiler, no server/Docker.
qdrant-client==1.12.2
fastembed==0.5.1               # local ONNX embeddings (free, offline, no torch)

# --- Phase 3: LLM providers -------------------------------------------------
google-generativeai==0.8.3     # Gemini (default)
openai==1.59.6                 # optional
anthropic==0.42.0              # optional
# ollama uses plain HTTP via httpx (below), no SDK required
httpx==0.28.1

# --- Phase 4: OCR fallback --------------------------------------------------
pytesseract==0.3.13            # requires Tesseract binary installed separately

# --- Phase 5: calculations --------------------------------------------------
numpy==2.2.1
scipy==1.15.0

# --- Phase 8: report export -------------------------------------------------
fpdf2==2.8.2                   # PDF generation (pure python, Windows-friendly)
arabic-reshaper==3.0.0         # Arabic letter shaping for PDF
python-bidi==0.6.3             # bidirectional (RTL) reordering for PDF
# python-docx / openpyxl (already above) handle Word / Excel export
